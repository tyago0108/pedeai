"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "@/lib/supabase/client";
type Pedido={id:string;total:number;status:string;pagamento:string;created_at:string};
const moeda=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"});
export function ControleFinanceiro(){const router=useRouter();const[pedidos,setPedidos]=useState<Pedido[]>([]);const[carregando,setCarregando]=useState(true);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user)return router.replace("/restaurante/login");const{data:perfil}=await supabase.from("perfis").select("empresa_id").single();if(!perfil)return;const{data}=await supabase.from("pedidos").select("id,total,status,pagamento,created_at").eq("empresa_id",perfil.empresa_id).order("created_at",{ascending:false});setPedidos((data??[]).map(p=>({...p,total:Number(p.total)})));setCarregando(false)})()},[router]);const inicioHoje=useMemo(()=>{const d=new Date();d.setHours(0,0,0,0);return d},[]);const inicioMes=useMemo(()=>new Date(inicioHoje.getFullYear(),inicioHoje.getMonth(),1),[inicioHoje]);const validos=pedidos.filter(p=>p.status!=="cancelado");const soma=(inicio:Date)=>validos.filter(p=>new Date(p.created_at)>=inicio).reduce((a,p)=>a+p.total,0);const porPagamento=Object.entries(validos.reduce<Record<string,number>>((a,p)=>({...a,[p.pagamento]:(a[p.pagamento]??0)+p.total}),{}));if(carregando)return <main className="grid min-h-screen place-items-center bg-stone-50">Carregando financeiro...</main>;return <main className="min-h-screen bg-stone-50 p-5 text-stone-900"><header className="mx-auto flex max-w-4xl items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-orange-500">PedeAI · Restaurante</p><h1 className="text-2xl font-bold">Controle financeiro</h1></div><Link href="/restaurante" className="rounded-xl bg-stone-900 px-3 py-2 text-sm font-bold text-white">Painel</Link></header><section className="mx-auto mt-6 grid max-w-4xl grid-cols-2 gap-3 md:grid-cols-4"><article className="rounded-2xl bg-stone-900 p-4 text-white"><p className="text-xs text-stone-300">Vendas hoje</p><p className="mt-2 text-xl font-bold">{moeda.format(soma(inicioHoje))}</p></article><article className="rounded-2xl bg-white p-4 shadow-sm"><p className="text-xs text-stone-500">No mês</p><p className="mt-2 text-xl font-bold">{moeda.format(soma(inicioMes))}</p></article><article className="rounded-2xl bg-white p-4 shadow-sm"><p className="text-xs text-stone-500">Pedidos pagos</p><p className="mt-2 text-xl font-bold">{validos.length}</p></article><article className="rounded-2xl bg-white p-4 shadow-sm"><p className="text-xs text-stone-500">Cancelados</p><p className="mt-2 text-xl font-bold">{pedidos.filter(p=>p.status==="cancelado").length}</p></article></section><section className="mx-auto mt-5 max-w-4xl rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-bold">Recebimentos por pagamento</h2><div className="mt-4 space-y-3">{porPagamento.map(([metodo,valor])=><div key={metodo} className="flex items-center justify-between border-b border-stone-100 pb-3"><span className="font-medium">{metodo}</span><strong className="text-orange-600">{moeda.format(valor)}</strong></div>)}</div></section><section className="mx-auto mt-5 max-w-4xl rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-bold">Últimos lançamentos</h2>{pedidos.slice(0,20).map(p=><div key={p.id} className="mt-3 flex justify-between border-b border-stone-100 pb-3 text-sm"><span>{new Date(p.created_at).toLocaleString("pt-BR")} · {p.pagamento}</span><strong className={p.status==="cancelado"?"text-red-600":"text-stone-900"}>{p.status==="cancelado"?"Cancelado":moeda.format(p.total)}</strong></div>)}</section></main>}
