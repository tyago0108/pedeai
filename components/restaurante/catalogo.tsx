"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "@/lib/supabase/client";
type Produto={id:string;nome:string;preco:number;imagem_url:string|null}; type Adicional={id:string;nome:string;preco:number};
export function CatalogoRestaurante(){const router=useRouter();const[empresa,setEmpresa]=useState('');const[produtos,setProdutos]=useState<Produto[]>([]);const[adicionais,setAdicionais]=useState<Adicional[]>([]);const[nome,setNome]=useState('');const[preco,setPreco]=useState('');const[imagem,setImagem]=useState<File|null>(null);const[nomeAd,setNomeAd]=useState('');const[precoAd,setPrecoAd]=useState('0');const[erro,setErro]=useState('');async function carregar(){const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/admin');return;}const{data:perfil}=await supabase.from('perfis').select('empresa_id').single();if(!perfil){setErro('Acesso de restaurante não encontrado.');return;}setEmpresa(perfil.empresa_id);const[a,b]=await Promise.all([supabase.from('produtos').select('id,nome,preco,imagem_url').eq('empresa_id',perfil.empresa_id).order('created_at',{ascending:false}),supabase.from('adicionais').select('id,nome,preco').eq('empresa_id',perfil.empresa_id).order('nome')]);setProdutos((a.data??[]).map(p=>({...p,preco:Number(p.preco)})));setAdicionais((b.data??[]).map(a=>({...a,preco:Number(a.preco)})));}useEffect(()=>{carregar();},[]);async function produto(e:FormEvent){e.preventDefault();if(!empresa)return;let imagem_url:string|null=null;if(imagem){const caminho=`${empresa}/${crypto.randomUUID()}-${imagem.name}`;const{error:up}=await supabase.storage.from('produto-imagens').upload(caminho,imagem);if(up)return setErro('Não foi possível enviar a imagem.');imagem_url=supabase.storage.from('produto-imagens').getPublicUrl(caminho).data.publicUrl;}const{error}=await supabase.from('produtos').insert({empresa_id:empresa,nome,preco:Number(preco),imagem_url,disponivel:true});if(error)return setErro('Não foi possível salvar o produto.');setNome('');setPreco('');setImagem(null);carregar();}async function adicional(e:FormEvent){e.preventDefault();const{error}=await supabase.from('adicionais').insert({empresa_id:empresa,nome:nomeAd,preco:Number(precoAd)});if(error)return setErro('Não foi possível salvar o adicional.');setNomeAd('');setPrecoAd('0');carregar();}async function excluir(id:string){await supabase.from('produtos').delete().eq('id',id);carregar();}return <main className="mx-auto min-h-screen max-w-4xl bg-stone-50 p-5 text-stone-900"><header><p className="text-sm font-bold uppercase tracking-widest text-orange-500">PedeAI · Restaurante</p><h1 className="mt-1 text-3xl font-bold">Cardápio e adicionais</h1><a href="/admin/pedidos" className="mt-2 inline-block text-sm font-semibold text-orange-600">Ver pedidos</a></header>{erro&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-red-700">{erro}</p>}<section className="mt-6 grid gap-5 md:grid-cols-2"><form onSubmit={produto} className="rounded-2xl bg-white p-5"><h2 className="font-bold">Novo produto</h2><div className="mt-4 space-y-2"><input required value={nome} onChange={e=>setNome(e.target.value)} placeholder="Nome" className="campo"/><input required type="number" min="0" step="0.01" value={preco} onChange={e=>setPreco(e.target.value)} placeholder="Preço" className="campo"/><input type="file" accept="image/*" onChange={e=>setImagem(e.target.files?.[0]??null)} className="block w-full text-sm"/><button className="w-full rounded-xl bg-orange-500 py-3 font-bold text-white">Adicionar produto</button></div></form><form onSubmit={adicional} className="rounded-2xl bg-white p-5"><h2 className="font-bold">Novo adicional</h2><div className="mt-4 space-y-2"><input required value={nomeAd} onChange={e=>setNomeAd(e.target.value)} placeholder="Ex.: Bacon, sem cebola" className="campo"/><input required type="number" min="0" step="0.01" value={precoAd} onChange={e=>setPrecoAd(e.target.value)} placeholder="Preço (0 para gratuito)" className="campo"/><button className="w-full rounded-xl bg-stone-900 py-3 font-bold text-white">Adicionar opção</button></div></form></section><section className="mt-6 rounded-2xl bg-white p-5"><h2 className="font-bold">Produtos</h2>{produtos.map(p=><div key={p.id} className="mt-3 flex items-center gap-3 border-b border-stone-100 pb-3">{p.imagem_url?<img src={p.imagem_url} alt="" className="h-12 w-12 rounded-lg object-cover"/>:<div className="h-12 w-12 rounded-lg bg-orange-100"/>}<span className="flex-1 font-medium">{p.nome} · R$ {p.preco.toFixed(2)}</span><button onClick={()=>excluir(p.id)} className="text-sm font-bold text-red-600">Excluir</button></div>)}<h2 className="mt-6 font-bold">Adicionais</h2>{adicionais.map(a=><p key={a.id} className="mt-2 text-sm">{a.nome} · R$ {a.preco.toFixed(2)}</p>)}</section></main>;}
