import { getSupabaseAdmin } from "@/lib/supabase/server";
export async function GET(request:Request){const codigo=new URL(request.url).searchParams.get("codigo");if(!codigo)return Response.json({error:"Pedido inválido."},{status:400});const db=getSupabaseAdmin();const{data}=await db.from("pedidos").select("id,cliente_nome,total,pagamento,empresas(nome,whatsapp,pix_chave)").eq("codigo_acompanhamento",codigo).maybeSingle();if(!data||data.pagamento!=="Pix")return Response.json({error:"Pagamento Pix não encontrado."},{status:404});const e=Array.isArray(data.empresas)?data.empresas[0]:data.empresas;return Response.json({cliente:data.cliente_nome,total:Number(data.total),restaurante:e?.nome,pixChave:e?.pix_chave,whatsapp:e?.whatsapp})}
